interface:
  display_name: PDF
  short_description: PDF document support
  icon_small: assets/preview.png
dependencies:
  tools:
    - type: mcp
      value: pdf-preview
      description: Optional preview service; detect and warn only.
      transport: stdio
      command: pdf-preview
policy:
  allow_implicit_invocation: true
  products:
    - eidos
